{
	//
	// this config is used to create the fqdn table against a mock dynamo instance runnin
	// on localhost.
	//
        "tableName" : "fqdn",
        "accessKey" : "foo",
        "secretKey" : "bar",
        "endPoint"  : "http://localhost:8000"
}
